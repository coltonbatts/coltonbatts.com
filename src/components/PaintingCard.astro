---
/**
 * PaintingCard.astro
 * ==================
 * Renders a project as a card in the portfolio gallery.
 *
 * The image sits inside a heavy-bordered "matted" frame with generous
 * internal padding. Below the frame, a monospaced label displays
 * the metadata: title, role, scope, year.
 *
 * Variants:
 *   - Default: standard card for grid placement
 *   - featured: larger frame, heavier shadow, more padding
 *
 * The card links to the individual project detail page.
 */

interface Props {
	title: string;
	medium: string;
	dimensions: string;
	year: number;
	image: string;
	slug: string;
	featured?: boolean;
	available?: boolean;
	series?: string;
	class?: string;
}

const {
	title,
	medium,
	dimensions,
	year,
	image,
	slug,
	featured = false,
	available = false,
	series,
	class: className = '',
} = Astro.props;
---

<a
	href={`/portfolio/${slug}`}
	class:list={[
		'painting-card group block',
		{ 'painting-card--featured': featured },
		className,
	]}
>
	<!-- The Frame: matted border with stamped shadow -->
	<div class="painting-card__frame">
		<div class="painting-card__mat">
			<div
				style="aspect-ratio:4/3;display:flex;align-items:center;justify-content:center;background:#1a1a1a;color:rgba(255,255,255,0.25);font-family:monospace;font-size:0.75rem;text-transform:uppercase;letter-spacing:0.1em;"
				class="painting-card__image"
			>
				{title}
			</div>
		</div>
	</div>

	<!-- Gallery Label: monospaced metadata block -->
	<div class="painting-card__label">
		<h3 class="painting-card__title">
			{title}
			{available && <span class="painting-card__available" title="Available">●</span>}
		</h3>
		<p class="painting-card__meta">
			{medium}, {year}
		</p>
		<p class="painting-card__dimensions">
			{dimensions}
		</p>
		{series && (
			<p class="painting-card__series">
				{series}
			</p>
		)}
	</div>
</a>
